
# Global portal server config

# Optional: still allow shared-key auth for quick testing
CUSTOMER_KEY=dev-shared-key

# --- Panel Server ---
PORT=3000
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/solomon
SESSION_SECRET=super-secret

# --- Multi-tenant keys (map tenants → allowed keys) ---
TENANTS=alexandracohomes,elevatedgarage

# You could store keys in DB, but for simple config:
ALEXANDRACOHOMES_KEY=dev-shared-key
ELEVATEDGARAGE_KEY=another-key
